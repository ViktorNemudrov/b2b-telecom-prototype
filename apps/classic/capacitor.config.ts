import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.b2btelecom.prototype",
  appName: "B2B Telecom Classic",
  webDir: "out"
};

export default config;
